# createDirectory

Create a directory, including any missing parents. Succeeds if it already exists.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `path` | string | Yes | Directory to create. Must be inside the working directory |

Returns `"directory created"` on success.

Full details: `readReference("createDirectory.md")`.
