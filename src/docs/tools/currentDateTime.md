# currentDateTime

Return the current date and time. Use it whenever a task involves "today", "now" or any
relative date — never guess the date.

No parameters.

Full details: `readReference("currentDateTime.md")`.
